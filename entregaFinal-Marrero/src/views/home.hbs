<div class="page-header">
    <h1>Product Catalog</h1>
</div>
<form action="/" method="GET" class="content-section">
    <div class="filters-grid">
        <!-- Input para 'limit' -->
        <div class="input-field">
            <label for="limit">Limit of products:</label>
            <input type="number" name="limit" id="limit" value="{{limit}}">
        </div>

        <div class="input-field">
            <label for="query">Search by category</label>
            <input type="string" name="query" id="query" value="{{query}}">
        </div>
        <!-- Input para 'page' -->
        <div class="input-field">
            <label for="page">Page:</label>
            <input type="number" name="page" id="page" value="{{page}}">
        </div>
        <!-- Selector para 'sort'-->
        <div class="input-field">
            <label for="sort">Order by price:</label>
            <select name="sort" id="sort" style="display: inline-block; padding: 5px; margin-left: 5px;">
                <option value="asc" {{#if isAsc}}selected{{/if}}>Ascendant</option>
                <option value="desc" {{#if isDesc}}selected{{/if}}>Descendant</option>
            </select>
        </div>

        <div class="input-field">
            <label for="stock">Stock:</label>
            <select name="stock" id="stock" style="display: inline-block; padding: 5px; margin-left: 5px;">
                <option value="true" {{#if isTrue}}selected{{/if}}>True</option>
                <option value="false" {{#if isFalse}}selected{{/if}}>False</option>
            </select>
        </div>

        <div class="actions-container">
            <button type="submit" class="btn-custom">Apply</button>
        </div>

    </div>
</form>


<div>
    <label for="carts">Carts:</label>
    <select id="carts" style="display: inline-block; padding: 5px; margin-left: 5px;">
        <option value="">--Select a cart--</option>
        {{#each carts}}
            <option value="{{this._id}}">{{this._id}}</option>
        {{/each}}
    </select>
    <button id="go-to-cart" class="btn-custom">View Cart</button>

</div>


<table border="1">
    <thead>
        <tr>
            <th>id</th>
            <th>Product</th>
            <th>Description</th>
            <th>Price</th>
            <th>Category</th>
            <th>Stock</th>
            <th>Acción</th>
        </tr>
    </thead>
    <tbody>
        {{#each products.payload}}
        <tr>
            <td>{{this._id}}</td>
            <td>{{this.title}}</td>
            <td>{{this.description}}</td>
            <td>{{this.price}}</td>
            <td>{{this.category}}</td>
            <td>{{this.stock}}</td>
            <td>
                <form method="POST" class="add-to-cart-form">
                    <input type="hidden" name="productId" value="{{this._id}}">
                    <button type="submit" class="agregar-btn">Add to cart</button>
                </form>
            </td>

        </tr>
        {{/each}}
    </tbody>
</table>



<script>
    document.addEventListener('DOMContentLoaded', function() {
        const cartSelect = document.getElementById('carts');

        document.querySelectorAll('.add-to-cart-form').forEach(function(form) {
            form.addEventListener('submit', async function(event) {
                event.preventDefault();

                const selectedCartId = cartSelect.value;
                if (!selectedCartId) {
                Swal.fire({
                    icon: 'error',
                    title: '¡Error!',
                    text: 'Please select a cart before adding a product.',
                });
                    return;
                }

                const productId = form.querySelector('input[name="productId"]').value;
                const formAction = `/api/carts/${selectedCartId}/product/${productId}`;



                try {
                    const response = await fetch(formAction, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                    });

                    if (response.ok) {
                        Swal.fire({
                        title: "Good job!",
                        text: `Product ${productId} was added to cart ${selectedCartId} successfully!`,
                        icon: "success"
                        });
                                            
                    } else {
                        Swal.fire({
                            icon: "error",
                            title: "Oops...",
                            text: "Something went wrong when we try to add the product!",
                        });
                    }
                } catch (error) {
                    console.error("Error al enviar la solicitud:", error);
                    alert('Error de conexión. Revisa tu conexión e intenta nuevamente.');
                }
            });
        });
    });

    document.addEventListener('DOMContentLoaded', function() {
    const cartSelect = document.getElementById('carts');
    const goToCartButton = document.getElementById('go-to-cart');
    goToCartButton.style.display = 'inline-block';

    cartSelect.addEventListener('change', function() {
        const selectedCartId = this.value;

        if (selectedCartId) {
            goToCartButton.dataset.cartId = selectedCartId;
        } else {
            goToCartButton.style.display = 'none';
        }
    });

    goToCartButton.addEventListener('click', function() {
        const selectedCartId = goToCartButton.dataset.cartId;

        if (selectedCartId) {
            window.location.href = `/cart/${selectedCartId}`;
        }
        });
    });

  
</script>
